#!/bin/sh
# Removing deleted remote branches and local branches with no references
# CAUTION: Removes local branches that have no remote counterpart

git fetch --prune && git branch --merged | grep -v master | xargs git branch -d
