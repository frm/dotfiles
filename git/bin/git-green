#!/usr/bin/env sh

description=`cat <<TXT
Pass the build. Stop, having it be failed.

Knock knock. It's the Mendes. With huge builds. And fixes. Fixed builds.

> _Pass the build. Stop, having it be failed._

said the Mendes.
TXT
`

git add . && git commit -m "$description"
