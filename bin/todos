#!/usr/bin/env sh

todos=`ls $HOME/Desktop/*.todo 2>/dev/null`

if [[ -z $todos ]]; then
  echo "[todo]: nothing at all! well done, lad!"
else
  for todo in $todos
  do
    echo $todo \
      | sed -e "s/^.*Desktop\/\(.*\).todo/\1/g" \
      | tr '_' ' '
  done
fi

